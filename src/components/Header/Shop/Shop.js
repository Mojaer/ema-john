import React from 'react';
import fakeData from '../../../fakeData/'

const Shop = () => {
    console.log(fakeData)
    return (
        <div>
            <h2>Shop</h2>
        </div>
    );
};

export default Shop;